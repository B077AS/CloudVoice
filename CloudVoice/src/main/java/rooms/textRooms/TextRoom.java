package rooms.textRooms;

import rooms.ChatRoom;
import serializedObjects.RoomsEnum;

public class TextRoom extends ChatRoom{

	public TextRoom(int ID, String name, int serverID, int port, RoomsEnum type) {
		super(ID, name, serverID, port, type);
		// TODO Auto-generated constructor stub
	}
}